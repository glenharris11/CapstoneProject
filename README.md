# House Value Calculator (Capstone)
This project includes a notebook consists of the model building process using python to create a model
to predict home values. The model was deployed locally using Flask which includes a home page, the 
calulator to input home features for a home value, an about page, and a page including my resume. 


